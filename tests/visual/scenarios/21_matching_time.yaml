id: matching_time
description: 'matching: Time #1'
view_path: matching-time
card:
  type: custom:auto-entities
  card:
    type: entities
    show_header_toggle: false
  filter:
    include:
    - domain: light
      last_changed: < 2 m ago
assertions:
- type: snapshot
  name: 21_matching_time
  threshold: 0.02
