id: unique_unique
description: 'unique: Unique'
view_path: unique-unique

setup:
  - type: ha_service
    domain: light
    service: turn_on
    data:
      entity_id: light.bed_light
      brightness: 255

card:
  type: custom:auto-entities
  card:
    type: entities
    show_header_toggle: false
  unique: true
  filter:
    include:
      - entity_id: light.bed_light
      - entity_id: light.bed_light
      - entity_id: light.bed_light

assertions:
  - type: snapshot
    name: 43_unique_unique
    threshold: 0.02
