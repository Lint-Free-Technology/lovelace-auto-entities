id: template_actual_template
description: 'template: Actual template #1'
view_path: template-actual-template
card:
  type: custom:auto-entities
  card: &id001
    type: entities
    show_header_toggle: false
  filter:
    include:
    - domain: group
      options:
        type: custom:auto-entities
        card: *id001
        filter:
          template: '{{state_attr(config.entity, ''entity_id'')}}'
assertions:
- type: snapshot
  name: 24_template_actual_template
  threshold: 0.02
