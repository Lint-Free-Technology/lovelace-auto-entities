id: rename_rename_per_filter_rename_with_eval_js_prepend_area
description: 'rename: Rename – per-filter rename with eval_js (prepend area) #1'
view_path: rename-rename-per-filter-rename-with-eval-js-prepend-area
card:
  type: custom:auto-entities
  card:
    type: entities
    show_header_toggle: false
  filter:
    include:
    - domain: sensor
      rename:
        method: friendly_name
        prepend: '${area ? area + '': '' : ''''}'
        eval_js: true
assertions:
- type: snapshot
  name: 43_rename_rename_per_filter_rename_with_eval_js_prepend_area
  threshold: 0.02
